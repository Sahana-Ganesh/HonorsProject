# Environment variables for Frame Select backend

# Performance settings
OMP_NUM_THREADS=1
MKL_NUM_THREADS=1
OPENBLAS_NUM_THREADS=1

# Optional: Custom storage paths (defaults to app/storage/)
# STORAGE_BASE_PATH=/custom/storage/path

# Optional: Custom scoring weights (defaults in config.py)
# SHARPNESS_WEIGHT=0.40
# COMPOSITION_WEIGHT=0.35
# EMOTION_WEIGHT=0.15
# ACTION_WEIGHT=0.10